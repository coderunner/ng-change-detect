# Change Detect

Démo pour expérimenter avec les stratégies de détection de changement d'Angular.

Pour utiliser la stratégie _OnPush_, il faut l'activer dans _button.component.ts_ et _app.component.ts_.
